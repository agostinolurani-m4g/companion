# Backlog HMR Companion

- **POI rifugi / Google Maps**: quando un rifugio o un POI ha un URL “place” di Google Maps (pagina luogo) oltre alle coordinate, preferire quel link nell’azione “Apri in Maps” invece del solo `?q=lat,lng`. Oggi i POI OSM usano spesso coordinate o `website` generico.
